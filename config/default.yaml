# Local Proof-Refactor defaults generated by configure.sh.
# Paths below are relative to workspace_dir unless absolute.

paths:
  workspace_dir: 'Lean'
  prompts_variant: 'light'
  output_dir: output
  dataset_dir: dataset
  session_logs_dir: output/session_logs
